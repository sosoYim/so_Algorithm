// function solution(prices, d, k) {
//   let answer = 0;
//   prices.sort((a, b) => a - b);

//   const average = arr => {
//     if (arr.length === 0) return 0;
//     else
//       return arr.reduce(
//         (acc, v, i) => (i === arr.length - 1 ? Math.floor((acc + v) / arr.length) : acc + v),
//         0
//       );
//   };

//   return answer;
// }

// console.log(solution([4, 5, 6, 7, 8], 4, 3)); // 6
