/**
 * @param {number} target
 * @param {number[]} nums
 * @return {number}
 */
var minSubArrayLen = function (target, nums) {
  let answer = 0;
  let lt = 0;

  return answer;
};

console.log(minSubArrayLen(7, [2, 3, 1, 2, 4, 3]));
